{% comment %}
You SHOULD NOT modify source code in this page because
It is automatically generated from Veda.
{% endcomment %}
{% assign locale_rtl = "ar,fa,he,ks,ps,sd,ur," %}
{% assign current_locale = localization.language.iso_code | append: ',' %}
<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}"
      {% if locale_rtl contains current_locale %}dir="rtl"{% endif %}>
<head>
    <title>{{ shop.name }}</title>
    {{ content_for_header }}

</head>
<body>
<div id="veda-root" class="veda-container">
    {{ content_for_layout }}
</div>
</body>
</html>
